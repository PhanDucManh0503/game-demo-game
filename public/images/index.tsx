const svgs = './svgs';
const imgs = './imgs';

const IMAGES:any ={
  logo: require(`${imgs}/logo.png`),
  DefaultAvatar: require(`${imgs}/default_avatar.png`),

}

const SVGS:any = {
  hLogo: require(`${svgs}/h_logo.svg`),
}

export {IMAGES,SVGS}